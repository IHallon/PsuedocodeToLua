-- Convert a user-input into all caps, print the result.
print("Please enter something") -- Prompt the user to enter something
local phrase = io.read() -- The user inputs something
local capitalize = string.upper(phrase) -- Converts all the lowercase letters of the user's input into uppercase letters
print(capitalize) -- Outputs the user's input now capitalize
