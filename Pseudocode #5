math.randomseed(os.time())
math.random() math.random() math.random()

evenCounter = 0
oddCounter = 0

for x = 1, 10 do
	randomNumber = math.random(1,100)
	print(randomNumber)
	if randomNumber % 2 == 0 then
		print("Even")
	else
		print("Odd")
	end
end

print("There were " .. evenCounter .. "even numbers and " .. evenCounter .. "odd numbers.")
